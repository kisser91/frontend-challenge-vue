const getters = {
  GET_THEME: (state) => state.theme,
  GET_USER_RESPONSE: (state) => state.userResponse,
  GET_REPORT_RESPONSE: (state) => state.reportResponse,
  GET_LOGIN_MENU: (state) => state.loginMenu,
};

export default getters;
